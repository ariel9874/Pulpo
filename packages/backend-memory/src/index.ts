/**
 * @batuta/backend-memory — implementación en memoria de `BackendPort`.
 *
 * Sirve para testear todo el sistema sin red ni Supabase. La implementación
 * real llega en la Etapa 3 (cuando exista la interfaz `BackendPort`).
 */

export const BACKEND_MEMORY_READY = false;
